# proyecto-1
cousera semana 3
Claro, aquí tienes un código de ejemplo en un flujo de trabajo utilizando git y GitHub para hacer un commit, una solicitud de extracción y una solicitud de extracción de mezcla:

```bash
# Hacer un commit
git add .
git commit -m "projecto 1"


# Archivo build.gradle (Módulo de app)
android {
    defaultConfig {
        resConfigs "es", "en", "fr", "de"
    }
}

# Archivo strings.xml
<resources>
    <string name="hello_world">Hello world!</string>
    <!-- Añade las traducciones en los diferentes idiomas -->
</resources>

# Fondo de pantalla: background_image.png
# Utiliza este archivo como fondo de pantalla en tu proyecto

# Archivo AndroidManifest.xml
<application
    android:icon="@drawable/ic_launcher"
    android:label="@string/app_name"
    android:theme="@style/AppTheme">
    <!-- Otros ajustes del manifiesto -->
</application>
```
